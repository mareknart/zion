import Card from "../ui/Card"
import { MainCard } from "./LoginCard.styled";
const LoginCard = (props)=>{

    return(
        <Card><MainCard>Login</MainCard></Card>
    )
}

export default LoginCard;